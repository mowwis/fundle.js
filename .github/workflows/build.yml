name: Production Build

on:
  push:
    branches: [ main ]
    paths:
      - 'src/**'

permissions:
  contents: write

jobs:
  bundle-and-minify:
    name: Bundle & Minify JS
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: '.github/workflows/*.yml'

      - name: Generate Distribution Files
        run: |
          mkdir -p dist
          npx esbuild src/index.js --bundle --format=iife --global-name=Fundle --outfile=dist/fundle.js
          npx esbuild src/index.js --bundle --format=iife --global-name=Fundle --minify --outfile=dist/fundle.min.js

      - name: Commit and push changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "build: auto-bundle and minify [skip ci]"
          file_pattern: 'dist/*'
          disable_globbing: false
